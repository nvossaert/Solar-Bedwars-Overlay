'use strict';
// TEMPLATE — copy this file to `secrets.js` (same folder) and fill in your keys.
//   cp src/main/secrets.example.js src/main/secrets.js
// secrets.js is gitignored so your keys never get committed. You can also just
// paste the keys in the app under Settings → API Keys (they save to userData, not the repo).
module.exports = {
  hypixelKey: '',      // from https://developer.hypixel.net
  urchinKey: '',       // your Urchin cubelify key (substituted into {key} in the endpoint URL)
  urchinAdminKey: '',  // Urchin admin key — only needed for the Blacklist Admin tab
};
